import {css} from 'styled-components'

export const absoluteCenterStyle = css`
  position: absolute;
  margin: auto;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
`

export const absoluteCenterStyleObj = {
  position: 'absolute',
  margin: 'auto',
  top: '0',
  bottom: '0',
  left: '0',
  right: '0',
}

